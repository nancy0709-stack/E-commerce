import React from "react";
import Authentication from "../components/authentication/authenticationForm/Authentication";

const AuthenticationPage: React.FC = () => {
  return <Authentication />;
};

export default AuthenticationPage;
